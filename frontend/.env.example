# ─── Formatly Frontend — Environment Variables (example) ────────────────────
#
# Rename to .env or create .env.local to override per machine.
# This file is safe to commit — it contains no secrets.
# ─────────────────────────────────────────────────────────────────────────────

# Backend API base URL
VITE_API_URL=http://127.0.0.1:8000
